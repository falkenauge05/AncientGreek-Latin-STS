# Evaluation Pipeline

Create sentence embeddings as described in the PaSeMiLL paper (and on github account).


Execute pipeline.sh with these arguments to start the entire pipeline.

    ./pipeline.sh input_file1.vec input_file2.vec hyperparameter gold_standard_file

NOTE: largely taken from PaSeMiLL paper